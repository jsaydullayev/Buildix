import { Package } from 'lucide-react';
import { ComingSoonPage } from '@/features/_shared/ComingSoonPage';

/** Товары и цены — catalog + price management, split from Склад. Phase A2. */
export default function ProductsPage() {
  return <ComingSoonPage titleKey="nav.products" icon={Package} />;
}
