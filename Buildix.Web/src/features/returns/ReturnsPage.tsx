import { Undo2 } from 'lucide-react';
import { ComingSoonPage } from '@/features/_shared/ComingSoonPage';

/** Возвраты — dedicated returns flow (SaleReturn, BE-3). Phase A5. */
export default function ReturnsPage() {
  return <ComingSoonPage titleKey="nav.returns" icon={Undo2} />;
}
