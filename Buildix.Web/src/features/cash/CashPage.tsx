import { Wallet } from 'lucide-react';
import { ComingSoonPage } from '@/features/_shared/ComingSoonPage';

/** Касса — cash ledger. Built out in phase A3 (CashMovement, BE-1). */
export default function CashPage() {
  return <ComingSoonPage titleKey="nav.cash" icon={Wallet} />;
}
